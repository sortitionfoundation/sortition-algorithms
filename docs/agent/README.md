# docs/agent/ directory

This directory contains research and plans the LLM agent has created while working on stories. It is **not** maintained - it just contains notes from the time those notes were written. So it might be interesting from a historical point of view, but there is always a good chance that the code has moved on since the files in this directory were written. So always examine the code as well as the document, and ultimately trust the code.
